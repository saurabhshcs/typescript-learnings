export function multiply(a: number, b: number): number {
    return a * b;
  }

  export function multiplyByTwo(a: number): number {
    return a * 2;
  }
export const A = 'A';

const getA = () => 'AA';

export default getA;